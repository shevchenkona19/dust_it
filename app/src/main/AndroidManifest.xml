<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dustit.clientapp">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.ui.activities.SplashActivity">

        </activity>
        <activity android:name=".mvp.ui.activities.ChooserActivity" />
        <activity android:name=".mvp.ui.activities.LoginActivity" />
        <activity android:name=".mvp.ui.activities.RegisterActivity">


        </activity>
        <activity android:name=".mvp.ui.activities.PreTestActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.ui.activities.TestActivity" />
        <activity android:name=".mvp.ui.activities.ResultActivity" />
        <activity android:name=".mvp.ui.activities.FeedActivity"/>
    </application>

</manifest>